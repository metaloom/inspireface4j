# Maven / Eclipse
target/
.classpath
.settings
.project
.vscode

# Model weights are fetched, never committed (see the module READMEs)
/yunet/models
/inspireface/packs
/jdlib/examples/*.dat
/jdlib/examples/*.bz2

# Native build tree (jdlib/jni is the source, jdlib/build is cmake output)
/jdlib/build

# Errors
hs_err*
